[pytest]
testpaths = tests
markers =
    slow: tests longs (walk-forward, optimiseur, benchmarks) exclus par défaut en CI (-m "not slow")
    strategy_smoke: tests smoke paramétrés parcourant l'ensemble des stratégies actives
