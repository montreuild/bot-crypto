# ================================================================
#  Crypto Bot V12 — Dépendances de DÉVELOPPEMENT
#
#  Séparées de requirements.txt parce qu'elles n'ont rien à faire dans
#  l'image de production : mypy (15 Mo), pytest et ses plugins, black, ruff
#  et leurs transitives (pygments…) pesaient ~70 Mo dans `crypto-bot:api`
#  sans jamais être exécutés par le conteneur de trading.
#
#  Installation en local (les deux fichiers) :
#    pip install -r requirements.txt -r requirements-dev.txt
#
#  Dans Docker : la cible `test` du Dockerfile les installe ; l'image de
#  production (cible `runtime`) ne les voit pas.
#    docker compose --profile test run --rm test
# ================================================================

# -- Tests -----------------------------------------------------------
pytest==9.1.1
pytest-cov==5.0.0          # Code coverage
pytest-benchmark==4.0.0    # Performance testing
httpx==0.27.0              # Client de test de l'API FastAPI

# -- Qualité de code -------------------------------------------------
black==26.3.1              # Code formatting
ruff==0.15.8               # Linting (remplace flake8/pyflakes, cf. ruff.toml)
mypy==1.8.0                # Type checking
