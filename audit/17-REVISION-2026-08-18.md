# Révision de l'audit — 18 août 2026 (reste X-03 / U-08 / U-05)

> Suite de [`16-REVISION-2026-08-18.md`](16-REVISION-2026-08-18.md).
> Branche `fix/audit-x03-u08-u05-reste`.

---

## 1. Livré

| Réf | Correction |
|---|---|
| **X-03** | Gel → `opt_freeze.py` (~184 L). Bayesian/Optuna → `opt_bayesian.py` (~328 L). `optimizer_search.py` ~817 L (`run`/grid/random/pool). `OptimizerSearchEngine` hérite des deux mixins. |
| **X-03 SMC** | Checkers + `_SignalCtx` → `smart_money_setups.py` (~506 L). `smart_money_signals.py` ~592 L (`_build_ctx` / `_score_setup` / `_signal_at`). Imports historiques conservés. |
| **U-08** | Tables Order Blocks / FVG / voids / rejections → `smart-graph-tables.tsx`. |
| **U-05** | `useTradingTimeframes` sans `as any`. Mutations backtest/replay typées. `BotStatus.git_commit`. `FastAnalysisResult`. `errorMessage` sur Smart Graph. |

## 2. Tailles

| Fichier | Avant #244 / #reste | Après |
|---|---|---|
| `optimizer_search.py` | 1 338 | ~817 + freeze 184 + bayesian 328 |
| `smart_money_signals.py` | 1 212 | ~592 + setups 506 |
| `smart-graph-view.tsx` | 1 324 | ~1 164 + tables 107 |

## 3. Encore ouvert

| Réf | Pourquoi |
|---|---|
| **X-02 / X-06** | Reportés (scoring v4/v5, 45 stratégies). |
| **U-08** | `smart-graph-view.tsx` encore ~1 164 L (hook chart). |
| **U-05** | `as any` graphiques (lightweight-charts), catch locaux, payloads hétérogènes. |
| **D-07** | Paper / `oos_tracker` sans trades live. |
