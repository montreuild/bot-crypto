# Révision de l'audit — 18 août 2026 (U-05 / U-08 / X-03)

> Suite de [`15-REVISION-2026-08-18.md`](15-REVISION-2026-08-18.md).
> PR `#244` — branche `fix/audit-u05-u08-x03`.
> Commentaires des fichiers touchés réduits aux contraintes non lisibles dans le code.

---

## 1. Livré

| Réf | Correction |
|---|---|
| **X-03** | `BacktestResult` → `app/engine/backtest_result.py` (~410 L). Cycle de vie (`_close_at`, `_close_partial_at`, `_manage_open_position`, `_try_enter`) → `PositionLifecycleMixin` (`position_lifecycle.py`, ~646 L). `backtest.py` garde `run()` + diagnostics (~686 L). Import inchangé : `from app.engine.backtest import Backtester, BacktestResult`. `bar_to_days` dans `app/core/timeframes.py`. |
| **U-08** | `/lab` = shell + `next/dynamic` par onglet (~175 L). BacktestTab / Verdict / BacktestResults → `components/views/backtest-view.tsx`. Optimizer : `JobCard`, `LiveProgress`, constantes dans `components/optimizer/`. Smart Graph : helpers (`smart-graph-helpers.ts`). |
| **U-05** | Contrats : `Bot.tf`, `edge.mean_pct` / `avg_return_pct`, `WalkForwardResult`, `MonteCarloResult`, `DualPassRuns`, `by_strategy`. Plus de `as any` métier sur bots, lab (Verdict / résultats), data (`flattenDatasets`), hooks optimize (status / start). Zod reste non bloquant. Schéma : `python scripts/export_openapi.py`. |

## 2. Tailles après découpage

| Fichier | Avant | Après |
|---|---|---|
| `app/engine/backtest.py` | 1 809 | ~686 |
| `app/engine/backtest_result.py` | — | ~410 |
| `app/engine/position_lifecycle.py` | — | ~646 |
| `frontend/src/app/lab/page.tsx` | 1 487 | ~175 |
| `frontend/src/components/views/backtest-view.tsx` | (dans lab) | ~1 191 |
| `frontend/src/components/views/optimizer-view.tsx` | 1 558 | ~1 015 + `job-card` 347 + `live-progress` 109 |
| `frontend/src/components/views/smart-graph-view.tsx` | 1 426 | ~1 324 + helpers |

## 3. Encore ouvert

| Réf | Pourquoi |
|---|---|
| **X-02 / X-06** | Reportés (duplication scoring v4/v5, 45 stratégies). |
| **X-03 reste** | `optimizer_search.py` (~1 338) et `smart_money_signals.py` (~1 212) non découpés. |
| **U-08 reste** | `smart-graph-view.tsx` encore > 1 200 L (helpers extraits seulement). |
| **U-05 reste** | `as any` de graphiques (lightweight-charts), catch locaux, payloads hétérogènes hors contrats. |
| **T-01 / T-02** | Tests de signification + modules sans test. |
| **D-07** | Paper / `oos_tracker` : aucun verdict tant que `live.n_trades == 0`. |

## 4. Tests et CI

- 110 + 36 pytest backtest / ML / ledger / journal / dual-pass / parité / cost model.
- Import : `Backtester` hérite de `PositionLifecycleMixin`.
- `test_risk_curve` lit `backtest.py` **et** `position_lifecycle.py`.
- Frontend : `eslint` (plus `next lint`), Node 22 (GitHub + GitLab).
- Types : `BackfillJob.job_id` / `univers` ; `StrategyStats` KPI optionnels ;
  `BacktestResult.limit` / `n_bars` ; `StrategyStats.trades` (compte) ≠
  `BacktestResult.trades` (liste).
