# Révision de l'audit — 18 août 2026 (revue-complete)

> Suite de [`19-REVISION-2026-08-18.md`](19-REVISION-2026-08-18.md).
> Source : [`2026-08-18-revue-complete/`](2026-08-18-revue-complete/).
> HEAD audité : `3ca68a3` (PR #248). **Livré** : PR **#249** (`fc195c6`),
> docs **#250**. Suite ARCH/SEC/API sur `fix/audit-arch-sec-api`.
> `data/backtest_history.json` et `data/oos_tracker.json` sont vides (`{}`).

---

## 1. Livré

### Immédiat + cette semaine

| Réf | Correction |
|---|---|
| **FIN-01** | `_close_at` lit `entry_fees` (jamais pollué par les jambes). |
| **FIN-02** | Sorties early/time : spread + taker, comme l'EOD. |
| **FIN-03** | `borrow_cost` accumulé, plus écrasé à la clôture. |
| **FIN-04** | Backtest appelle `ledger.update_risk` après trailing. |
| **FIN-05** | Plafond slot sur le cumul ; `reserve` rebind `slot_key` par stratégie (B-02). |
| **FIN-06** | Pyramidage × courbe de DD × frein de volatilité. |
| **FIN-07** | `ledger.resize` après jambe partielle. |
| **FIN-08** | Point mort via `venue_trade_cost`. |
| **FIN-09** | `round_trip_cost(..., cible=)`. |
| **FIN-10** | `venue=None` → `borrowed_notional(..., max_leverage=1)`. |
| **OPT-01/02** | `deflated_sharpe_gate` défaut `false`. |
| **OPT-03** | `dd_factor = 1/(1+dd/15)` ; `beats_baseline` refuse un DD +25 %. |
| **OPT-04** | Sans holdout : pas d'auto-apply. |
| **OPT-05** | Gate WF non évaluable → `False`. |
| **OPT-06** | Win-rate seul insuffisant. |
| **BT-01** | Fold OOS préfixé du warmup. |
| **BT-02** | WF hérite `backtest.realistic_risk`. |
| **BT-03** | `n_folds_failed` / `erreurs`. |
| **BT-04** | MTM ignore `bar > i`. |
| **BT-05** | Série B&H depuis `warmup+1`. |
| **BT-06** | `max_dd_p95_par_trade`. |
| **BT-07** | `hypothese` i.i.d. documentée. |
| **BT-09** | Historique court → OOS vide explicite. |
| **BT-10** | `is_nested: true` dans la sortie WF. |
| **LIVE-01** | Annulation stop : 3 issues ; échec relance l'id. |
| **LIVE-02** | `_order_failed` refuse open/new/pending à `filled=0`. |
| **LIVE-03** | Paper applique `partial_fill_pct`. |
| **LIVE-04** | `.get("paper_mode", True)` sur les sites d'ordre. |
| **LIVE-05** | `ScoreFactor` retiré du log d'ouverture. |
| **LIVE-06** | Rollback / notif : `logger.error` au lieu de `pass`. |
| **PERF-01** | Repli HTF irrégulier mémoïsé. |
| **PERF-02** | Repli `bb_squeeze` déjà O(lookback) — documenté, pas de change. |
| **ML-01** | IC Hanley–McNeil ; borne basse < 0,50 bloque. |
| **ML-02** | `chrono_split` via `default_purge_embargo`. |
| **ML-03** | `fallback_to_inline` au niveau racine ; `fit_trace` causal (pas P0). |
| **API-02 / SEC-01** | `err_id` opaque (ml / backtest / data / optimizer). |
| **TEST-01** | `--cov-fail-under=64`. |
| **TEST-02** | CI frontend : `test:coverage` + plancher 5 %. |
| **TEST-06** | eslint ignore `coverage/` / `.next`. |
| **UX-01** | `formatMoney` + `quoteCurrency` câblés (plus seulement l'alias). |
| **FE-04** | `'use client'` sur 6 modules à hooks. |
| **DATA-03** | Écarts OHLCV pré-calculés (diff vectorisé). |
| **ARCH-04** | `LiveHost` + mypy core/engine ; 24 `ignore_errors` restants. |

### Tests

`tests/test_revue_2026_08_18.py` + mises à jour `test_partial_exits`,
`test_execution_parity`, `test_ml_splitting`, `test_venue_costs`,
`test_risk_ledger`. Frontend : `lib/__tests__/utils.test.ts`, `api.test.ts`.

---

## 2. Encore ouvert

### À faire — enjeu fort

| Réf | Effort | Descriptif |
|---|---|---|
| **ML-03** (mesure) | ✅ | Causal : `prepare_for_backtest` ne fite pas ; `score()` entraîne sur `aligned_train_window(ctx.window)`. Trace `ml_info.fit_trace` ; `any_full_series` serait un P0. |
| **FE-01 / TEST-02** | en cours | Tests `lib/api.ts` + `hooks/use-api.ts` (fetch mock, pas MSW). Plancher CI 5 % ; objectif 30 % encore ouvert (vues). |
| **UX-01** (câblage) | ✅ | `quoteCurrency()` + `formatMoney` sur backtest, compare, portefeuille, positions, trades, topbar, bandeau. `formatUSD` reste l'alias. |
| **FE-02 / UX-03 / UX-04** | ✅ | Config : `isLoading`/`isError` + toast lecture ; mutations déjà en sonner. Compare / backtest-results : en cours ≠ vide ≠ erreur. |
| **TEST-05 / PERF-03** | ✅ | `gapped_ohlcv` + test slow `trend` ≥ 150 barres/s (parquet `data/ohlcv` si présent, sinon série à trous). |

### Livré ensuite (ARCH / SEC / API)

| Réf | Correction |
|---|---|
| **SEC-02** | `?api_key=` retiré. `POST /api/ws/ticket` + `?ticket=` (usage unique, 30 s). |
| **ARCH-01** | `BacktestPayload` dataclass ; `to_dict()` = `asdict`. |
| **ARCH-02** | `_evaluer_sorties` / `_appliquer_jambes` / `_mettre_a_jour_trailing`. |
| **API-01 / FE-03** | `response_model` 5 routes ; `generated.ts` = tous les modèles publics de `schemas.py`. |
| **ARCH-03** | `app/core/smc/` et `app/core/risk/` ; shims aux anciens chemins. |
| **ARCH-04 / TEST-03** | `OptimizerHost` / `LifecycleHost`. Mypy `app/core` + `app/engine` (bloquant) ; 24 modules encore `ignore_errors`. |
| **ML-05** | Test : `fit()` qui échoue n'est pas relancé à chaque barre. |
| **ML-04** | `auc_amp` = eval, plus early-stop / isotonie. |
| **ML-03** | Mesure : inline causal ; `ml_info.fit_trace`. |
| **UX-01 / FE-02 / UX-03 / UX-04** | Devise réelle ; toasts config ; loading compare/backtest. |
| **TEST-05 / PERF-03** | Débit backtest sur série à trous (garde PERF-01). |
| **ML-04** | AUC publiée sur la tranche eval, plus sur le calibrage. |
| **FE-01** (amorce) | Tests `api.ts` / `use-api.ts`. |
| **ARCH-04** (suite) | 9 modules mypy de plus ; 24 `ignore_errors`. |
| **FE-03** (suite) | Tous les modèles `schemas.py` dans `generated.ts`. |

### À faire — chantier structurant

| Réf | Effort | Descriptif |
|---|---|---|
| **ARCH-04** (reste) | 24 | 14 modules levés au total. Le reste (backtest, database, mixins risk/lifecycle, indicateurs, config…) encore `ignore_errors`. |
| **FE-03** (reste) | partiel | Tous les schémas Pydantic de `app.api.schemas` sont générés. `index.ts` reste pour les contrats UI plus riches (BotStatus, CostModel, vues). |
| **ML-04** | ✅ | Train / calib (early-stop + isotonie) / eval (AUC publiée). `auc_source=eval`. |

### À faire — moindre urgence

| Réf | Effort | Descriptif |
|---|---|---|
| **API-03** | 1 j | Corps typés (`Field(ge=…, le=…)`) sur les POST de configuration risque. |
| **API-04 / TEST-04** | 2 j | `scanner_service.py` à 4 % de couverture — produit les signaux affichés. |
| **UX-02** | 2 h | `Intl.NumberFormat('fr-FR')` — **régénérer** les snapshots Playwright. |
| **PERF-05** | ½ j | Profiler `smart_money` 1 h (débit ÷5,1 sans `htf_trend`) : hooks O(n) ou coût SMC. |
| **FIN-11** | mesure | Courbe de risque en marches (×1 / ×0,75 / ×0,5). Mesurer une rampe 5–15 % avant de changer. |
| **U-05** (reste) | — | `as any` lightweight-charts / primitives SMC / Recharts ; payload replay hétérogène. |

### Décisions, pas du code

| Réf | Descriptif |
|---|---|
| **DATA-01** | Feature store peuplé pour 1 symbole / ~600. Cache opportuniste, ou job de pré-calcul ? |
| **DATA-02** | ~590 répertoires d'actions, `can_execute: false` (G3 absent). Conserver ou alléger ? |
| **DATA-04** | `oos_tracker.json` et `optimizer_changelog.json` encore suivis par git. |
| **D-07** | Verdicts `oos_tracker` : il faut du paper avec des trades live, pas un correctif. |

### Reportés (consigne)

| Réf | Descriptif |
|---|---|
| **X-02** | `scoring_statistique_opus_v5` duplique `_v4` à ~80 %. |
| **X-06** | 45 stratégies à inventorier / retirer. |

---

## 3. Chaîne de mesure

FIN-01 / FIN-02 / BT-01 changent les chiffres des backtests existants.
`backtest_history` et `oos_tracker` sont à `{}` (history gitignoré ; tracker
suivi, déjà vide sur `main`). Revalider les paramètres déjà retenus par
l'optimiseur avant de les réappliquer.
