# Révision de l'audit — 18 août 2026 (revue-complete)

> Suite de [`19-REVISION-2026-08-18.md`](19-REVISION-2026-08-18.md).
> Source : [`2026-08-18-revue-complete/`](2026-08-18-revue-complete/).
> Branche `fix/audit-revue-2026-08-18`. HEAD audité : `3ca68a3` (PR #248).

---

## 1. Livré

### Immédiat + cette semaine

| Réf | Correction |
|---|---|
| **FIN-01** | `_close_at` lit `entry_fees` (jamais pollué par les jambes). |
| **FIN-02** | Sorties early/time : spread + taker, comme l'EOD. |
| **FIN-03** | `borrow_cost` accumulé, plus écrasé à la clôture. |
| **FIN-04** | Backtest appelle `ledger.update_risk` après trailing. |
| **FIN-05** | Plafond slot sur le cumul ; `reserve` rebind `slot_key` par stratégie (B-02). |
| **FIN-06** | Pyramidage × courbe de DD × frein de volatilité. |
| **FIN-07** | `ledger.resize` après jambe partielle. |
| **FIN-08** | Point mort via `venue_trade_cost`. |
| **FIN-09** | `round_trip_cost(..., cible=)`. |
| **FIN-10** | `venue=None` → `borrowed_notional(..., max_leverage=1)`. |
| **OPT-01/02** | `deflated_sharpe_gate` défaut `false`. |
| **OPT-03** | `dd_factor = 1/(1+dd/15)` ; `beats_baseline` refuse un DD +25 %. |
| **OPT-04** | Sans holdout : pas d'auto-apply. |
| **OPT-05** | Gate WF non évaluable → `False`. |
| **OPT-06** | Win-rate seul insuffisant. |
| **BT-01** | Fold OOS préfixé du warmup. |
| **BT-02** | WF hérite `backtest.realistic_risk`. |
| **BT-03** | `n_folds_failed` / `erreurs`. |
| **BT-04** | MTM ignore `bar > i`. |
| **BT-05** | Série B&H depuis `warmup+1`. |
| **BT-06** | `max_dd_p95_par_trade`. |
| **BT-07** | `hypothese` i.i.d. documentée. |
| **BT-09** | Historique court → OOS vide explicite. |
| **BT-10** | `is_nested: true` dans la sortie WF. |
| **LIVE-01** | Annulation stop : 3 issues ; échec relance l'id. |
| **LIVE-02** | `_order_failed` refuse open/new/pending à `filled=0`. |
| **LIVE-03** | Paper applique `partial_fill_pct`. |
| **LIVE-04** | `.get("paper_mode", True)` sur les sites d'ordre. |
| **LIVE-05** | `ScoreFactor` retiré du log d'ouverture. |
| **LIVE-06** | Rollback / notif : `logger.error` au lieu de `pass`. |
| **PERF-01** | Repli HTF irrégulier mémoïsé. |
| **PERF-02** | Repli `bb_squeeze` déjà O(lookback) — documenté, pas de change. |
| **ML-01** | IC Hanley–McNeil ; borne basse < 0,50 bloque. |
| **ML-02** | `chrono_split` via `default_purge_embargo`. |
| **ML-03** | `fallback_to_inline` au niveau racine de `to_dict()`. |
| **API-02 / SEC-01** | `err_id` opaque (ml / backtest / data / optimizer). |
| **TEST-01** | `--cov-fail-under=64`. |
| **TEST-02** | CI frontend : `test:coverage` + plancher 5 %. |
| **TEST-06** | eslint ignore `coverage/` / `.next`. |
| **UX-01** | `formatMoney(value, currency)` ; `formatUSD` alias. |
| **FE-04** | `'use client'` sur 6 modules à hooks. |
| **DATA-03** | Écarts OHLCV pré-calculés (diff vectorisé). |
| **ARCH-04** | `app/live/protocols.py` (`LiveHost`) hérité par les 4 mixins. |

### Tests

`tests/test_revue_2026_08_18.py` + mises à jour `test_partial_exits`,
`test_execution_parity`, `test_ml_splitting`, `test_venue_costs`,
`test_risk_ledger`. Frontend : `lib/__tests__/utils.test.ts`, `api.test.ts`.

---

## 2. Encore ouvert

| Réf | Pourquoi |
|---|---|
| **ML-03** (mesure) | Le repli est visible ; la causalité de `fit()` inline n'est pas tranchée. |
| **ML-04** | 3 tranches train/calib/éval — ½ j, change les AUC publiées. |
| **ML-05** | Test de garde `fit()` une fois — à ajouter avec la mesure ML-03. |
| **ARCH-01** | `BacktestResult` dataclass / OpenAPI — 2-3 j. |
| **ARCH-02** | Découper `_manage_open_position`. |
| **ARCH-03** | Sous-paquets `core/smc`, `core/risk`. |
| **API-01 / FE-03** | `response_model` + types générés. |
| **API-03 / API-04** | Schémas d'entrée restants ; couverture scanner. |
| **TEST-05 / PERF-03** | Garde de débit sur série réelle (données hors ce worktree). |
| **TEST-03** | Mypy au-delà de 3 fichiers — après ARCH-04 étendu. |
| **FE-01 / TEST-02** | 30 % frontend : plancher posé à 5 %, à monter. |
| **FE-02 / UX-03 / UX-04** | `config-view` a déjà `QueryBoundary` ; compare/backtest-results à compléter. |
| **UX-01** (câblage) | `formatMoney` existe ; `quote_currency` pas encore propagé partout. |
| **UX-02** | `fr-FR` casserait les snapshots visuels. |
| **FIN-11** | Courbe de risque en marches — à mesurer, pas à changer. |
| **DATA-01 / DATA-02 / DATA-04 / SEC-02** | Décisions, pas du code. |
| **PERF-05** | `smart_money` 5,1× — à profiler. |
| **X-02 / X-06** | Reportés (consigne). |

---

## 3. Chaîne de mesure

FIN-01 / FIN-02 / BT-01 changent les chiffres des backtests existants.
`backtest_history` / `oos_tracker` à vider après merge.
