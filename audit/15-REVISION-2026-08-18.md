# Révision de l'audit — 18 août 2026 (soir)

> Suite de [`14-REVISION-2026-08-18.md`](14-REVISION-2026-08-18.md).
> Branche `fix/audit-r01-r02-x01-u05-p01` sur `main` @ `#242`.
> CI a11y + visual vertes après le remap de contraste.

---

## 1. Livré dans cette passe

| Réf | Correction |
|---|---|
| **R-01** | Sharpe live : `None` sous `MIN_SIGNIFICANT_TRADES` (10), plus `0.0` dès 3. Même plancher sur `/api/trades` (slot). `/portfolio` : `MetricValue` — plus de `.toFixed` sur `null`. |
| **R-02** | Backtest : `RiskLedger.reserve` / `release` / `resize` (scale-in). Sans enveloppe de slot, les agrégats symbole/venue restent larges (comportement historique). Avec enveloppe, F-05 / L-05 s'appliquent aussi au backtest. |
| **X-01** | Les six modules listés : 5 déjà câblés (i18n, risk gate, dual pass, `venue_envelope`, overfitting_gate). L'heuristique morte `app/core/deflated_sharpe.py` est **supprimée** ; le gate utilise `opt_scoring.deflated_sharpe_ratio` (Bailey). |
| **U-05** | Première passe : `sharpe` nullable, types `sim`/`live`/`monte_carlo`, `export_openapi.py`. Complété en #244 (`audit/16`) : `tf` / `mean_pct`, WF/MC/`runs`, plus de `as any` métier. |
| **P-01** | `BaseStrategy.prepare_for_backtest` mémorise le frame (O(n) une fois). Combiné à P-05 (`df.slice` + `bar_index`) et aux `prepare` déjà présents sur les stratégies lourdes. La boucle barre-à-barre reste en Python. |

## 2. Encore ouvert (hors cette demande)

| Réf | Pourquoi |
|---|---|
| **X-02 / X-06** | Reportés explicitement (duplication scoring v4/v5, 45 stratégies). |
| **U-08 / X-03** | ✅ backtest + lab (#244). Restent `optimizer_search.py`, `smart_money_signals.py`, `smart-graph-view.tsx`. Voir [`16`](16-REVISION-2026-08-18.md). |
| **T-01 / T-02** | Tests de signification + modules sans test. |
| **D-07** | Paper / `oos_tracker` : aucun verdict tant que `live.n_trades == 0`. |

## 3. Tests

- `tests/test_health_sharpe.py` — plancher 10, `None` sous le seuil.
- `tests/test_backtest_risk_ledger.py` — refus `notionnel_min` via le ledger.
