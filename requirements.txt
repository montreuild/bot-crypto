# ═══════════════════════════════════════════════════════════════
#  Crypto Bot V9 — Dépendances Python
#  !! Python 3.12 OBLIGATOIRE !!
#
#  Installation sur Ubuntu 22.04 (Python 3.10 par défaut) :
#    sudo add-apt-repository ppa:deadsnakes/ppa -y
#    sudo apt install -y python3.12 python3.12-venv python3.12-dev
#    python3.12 -m venv /opt/crypto_bot/.venv
#    source /opt/crypto_bot/.venv/bin/activate
#    pip install -r requirements.txt
#
#  Sur Ubuntu 24.04 (Python 3.12 natif) :
#    python3.12 -m venv /opt/crypto_bot/.venv
#    source /opt/crypto_bot/.venv/bin/activate
#    pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Web / API ─────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.12

# ── Exchange ──────────────────────────────────────────────────
ccxt>=4.3.0

# ── Data / Calcul ─────────────────────────────────────────────
# numpy 2.x est supporté par Python 3.12 et compatible pandas 2.2+
pandas>=2.2.0
numpy>=2.0.0
pyyaml>=6.0.2

# ── Base de données ───────────────────────────────────────────
sqlalchemy>=2.0.30

# ── Machine Learning (optionnel — nécessaire si ml.enabled: true) ─
# scikit-learn 1.5+ est la première version avec support Python 3.12 officiel
scikit-learn>=1.5.0
# joblib est inclus avec scikit-learn

# ── Notifications ─────────────────────────────────────────────
requests>=2.32.0

# ── Développement / Debug (optionnel) ─────────────────────────
pytest>=8.2.0
httpx>=0.27.0          # Pour tester l'API FastAPI
